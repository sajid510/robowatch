# Assets

Repository media used by the README and documentation:

- `architecture.svg` — RoboWatch pipeline diagram (embedded in README).
- Sample output lives in [`examples/sample-report.html`](../examples/sample-report.html)
  (generated by `python -m src.main --offline --dry-run --output-dir examples`).

To add screenshots, drop images here and reference them from `../docs/` or the
README.
